#!/usr/bin/env bash
# Installs Home Assistant core + test deps into the devcontainer's Python env.
set -euo pipefail
cd "$(dirname "$0")/.."

python3 -m pip install --upgrade pip
pip install -r requirements_test.txt
